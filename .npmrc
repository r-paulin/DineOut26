# npm (local / tools that still use npm): tolerate Bolt + React 19 peer skew
legacy-peer-deps=true

# pnpm (Cloudflare Pages prefers pnpm-lock.yaml when present): same idea, explicit
strict-peer-dependencies=false
