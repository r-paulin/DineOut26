import * as React from "react";
const IconViewContext = /*#__PURE__*/React.createContext(undefined);
export function IconViewProvider({
  size,
  children
}) {
  const value = React.useMemo(() => ({
    size
  }), [size]);
  return /*#__PURE__*/React.createElement(IconViewContext.Provider, {
    value: value
  }, children);
}
export function useIconViewContext() {
  const context = React.useContext(IconViewContext);
  if (!context) {
    throw new Error("IconView components must be used within an IconViewProvider");
  }
  return context;
}