function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Avatar as BaseUIAvatar } from "@base-ui/react/avatar";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useIconViewContext } from "./context/IconViewContext";
import { CLASSNAME_TW_ICON_SIZE } from "./shared-styles";
const CLASSNAME_TW_FALLBACK = "flex items-center justify-center text-primary";
export const IconViewFallback = /*#__PURE__*/React.forwardRef(function IconViewFallback(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const {
    size
  } = useIconViewContext();
  const getFallbackClassName = React.useCallback(state => cx(CLASSNAME_TW_FALLBACK, CLASSNAME_TW_ICON_SIZE[size], addConsumerClasses({
    ...state,
    size
  }, className)), [className, size]);
  return /*#__PURE__*/React.createElement(BaseUIAvatar.Fallback, _extends({}, rest, {
    ref: forwardedRef,
    className: getFallbackClassName
  }));
});