function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Avatar as BaseUIAvatar } from "@base-ui/react/avatar";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useIconViewContext } from "./context/IconViewContext";
import { CLASSNAME_TW_ICON_SIZE } from "./shared-styles";
const CLASSNAME_TW_ICON_WRAPPER = "flex items-center justify-center text-primary";
const CLASSNAME_TW_ICON = "h-full w-full";
export const IconViewIcon = /*#__PURE__*/React.forwardRef(function IconViewIcon(props, forwardedRef) {
  const {
    className,
    icon,
    ...rest
  } = props;
  const {
    size
  } = useIconViewContext();
  const getIconClassName = React.useCallback(state => cx(CLASSNAME_TW_ICON_WRAPPER, CLASSNAME_TW_ICON_SIZE[size], addConsumerClasses({
    ...state,
    size
  }, className)), [className, size]);
  const content = React.useMemo(() => {
    const iconClassName = cx(CLASSNAME_TW_ICON, icon.props.className);
    const ariaHidden = icon.props["aria-hidden"];
    return /*#__PURE__*/React.cloneElement(icon, {
      className: iconClassName,
      "aria-hidden": ariaHidden ?? true
    });
  }, [icon]);
  return /*#__PURE__*/React.createElement(BaseUIAvatar.Fallback, _extends({}, rest, {
    ref: forwardedRef,
    className: getIconClassName
  }), content);
});