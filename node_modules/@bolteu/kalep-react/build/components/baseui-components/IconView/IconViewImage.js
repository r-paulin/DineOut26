function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Avatar as BaseUIAvatar } from "@base-ui/react/avatar";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useIconViewContext } from "./context/IconViewContext";
const CLASSNAME_TW_IMAGE = "block h-full w-full object-cover";
export const IconViewImage = /*#__PURE__*/React.forwardRef(function IconViewImage(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const {
    size
  } = useIconViewContext();
  const getImageClassName = React.useCallback(state => cx(CLASSNAME_TW_IMAGE, addConsumerClasses({
    ...state,
    size
  }, className)), [className, size]);
  return /*#__PURE__*/React.createElement(BaseUIAvatar.Image, _extends({}, rest, {
    ref: forwardedRef,
    className: getImageClassName
  }));
});