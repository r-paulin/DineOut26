import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useButtonContext } from "./context/useButtonContext";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
/**
 * ### Button.Label
 * Use this for the `Button` content in case you need to include icons.
 *
 * Using the `Button.Label` subcomponent with `Button.Icon` components
 * ensures, that the leading and trailing icons get the correct styles applied
 * ```tsx
 *  <Button>
 *      <Button.Icon icon={<Heart />} />
 *      <Button.Label>Both Icons</Button.Label>
 *      <Button.Icon icon={<Heart />} />
 *  </Button>
 * ```
 */
export const ButtonLabel = /*#__PURE__*/React.forwardRef(function ButtonLabel(props, ref) {
  const {
    render,
    className,
    ...rest
  } = props;
  const {
    state
  } = useButtonContext();
  const defaultProps = {
    className: cx(addConsumerClasses(state, className))
  };
  return useRender({
    defaultTagName: "span",
    ref,
    render,
    state,
    props: mergeProps(defaultProps, rest)
  });
});