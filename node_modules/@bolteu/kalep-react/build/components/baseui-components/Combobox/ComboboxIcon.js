function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import ChevronDown from "@bolteu/kalep-react-icons/dist/ChevronDown";
import { useComboboxContextSelector, useComboboxInputContextSelector } from "./context";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Combobox.Icon
 * Chevron/indicator rendered inside `Combobox.Trigger`.
 *
 * ```tsx
 * <Combobox.Trigger>
 *   <Combobox.Icon />
 * </Combobox.Trigger>
 * ```
 */
export const ComboboxIcon = /*#__PURE__*/React.forwardRef(function ComboboxIcon(props, forwardedRef) {
  const size = useComboboxContextSelector(context => context.customComboboxProps.size);
  const disabled = useComboboxInputContextSelector(context => context.comboboxInputState?.disabled);
  const open = useComboboxInputContextSelector(context => context.comboboxInputState?.open);
  const {
    className,
    render,
    ...rest
  } = props;
  const getComboboxIconClassNames = React.useCallback(state => cx(`transition-transform duration-100 ease-linear`, open && "rotate-180", disabled ? "text-tertiary" : "text-primary", addConsumerClasses(state, className)), [className, disabled, open]);
  const renderDefault = React.useCallback(iconProps => {
    return /*#__PURE__*/React.createElement(ChevronDown, _extends({}, iconProps, {
      size: size === "sm" ? "md" : "lg",
      "aria-hidden": "true"
    }));
  }, [size]);
  return /*#__PURE__*/React.createElement(BaseUICombobox.Icon, _extends({}, rest, {
    ref: forwardedRef,
    className: getComboboxIconClassNames,
    render: render ?? renderDefault
  }));
});