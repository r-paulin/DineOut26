function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { useRender } from "@base-ui/react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import { useComboboxContextSelector } from "./context";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { ComboboxPositionerContextProvider } from "./context/ComboboxPositionerContextProvider";
function PositionerElement(props) {
  const {
    render,
    state,
    baseUIInternalRef,
    wrapperRef,
    ...rest
  } = props;
  const refs = [baseUIInternalRef, wrapperRef].filter(ref => ref !== undefined);
  return useRender({
    defaultTagName: "section",
    render,
    ref: refs,
    state,
    props: rest
  });
}

/**
 * ### Combobox.Positioner
 * Anchors `Combobox.Popup` relative to the input wrapper or a custom anchor (like the chips container).
 *
 * ```tsx
 * <Combobox.Portal>
 *   <Combobox.Positioner anchor={chipsRef} sideOffset={4}>
 *     <Combobox.Popup>…</Combobox.Popup>
 *   </Combobox.Positioner>
 * </Combobox.Portal>
 * ```
 *
 * Override `anchor` when chips should control placement; otherwise it falls back to the input wrapper.
 */
export const ComboboxPositioner = /*#__PURE__*/React.forwardRef(function ComboboxPositioner(props, forwardedRef) {
  const {
    className,
    anchor,
    render: userRender,
    ...rest
  } = props;
  const controlRef = useComboboxContextSelector(context => context.inputWrapperRef);
  const getComboboxPositionerClassNames = React.useCallback(state => cx("max-h-80 w-max rounded-md shadow-md", addConsumerClasses(state, className)), [className]);
  const defaultRender = React.useCallback((positionerProps, state) => {
    const {
      ref,
      ...restPositionerProps
    } = positionerProps;
    return /*#__PURE__*/React.createElement(ComboboxPositionerContextProvider, {
      comboboxPositionerState: state
    }, /*#__PURE__*/React.createElement(PositionerElement, _extends({}, restPositionerProps, {
      render: userRender,
      baseUIInternalRef: ref,
      wrapperRef: forwardedRef,
      state: state
    })));
  }, [forwardedRef, userRender]);
  return /*#__PURE__*/React.createElement(BaseUICombobox.Positioner, _extends({
    sideOffset: 5
  }, rest, {
    ref: forwardedRef,
    anchor: anchor ?? controlRef,
    className: getComboboxPositionerClassNames,
    render: defaultRender
  }));
});