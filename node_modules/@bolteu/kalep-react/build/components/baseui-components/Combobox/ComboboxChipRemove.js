function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import Clear from "@bolteu/kalep-react-icons/dist/Clear";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Combobox.ChipRemove
 * Button rendered inside each chip to remove a selected option.
 *
 * ```tsx
 * <Combobox.Chip>
 *   {option.label}
 *   <Combobox.ChipRemove aria-label={`Remove ${option.label}`} />
 * </Combobox.Chip>
 * ```
 */
export const ComboboxChipRemove = /*#__PURE__*/React.forwardRef(function ComboboxChipRemove(props, forwardedRef) {
  const {
    className,
    render,
    ...rest
  } = props;
  const getComboboxChipRemoveClassNames = React.useCallback(state => {
    return cx(`
                  flex cursor-pointer items-center justify-center rounded-full border-0 bg-transparent p-0
                  text-secondary transition-colors duration-100 ease-linear
                  focus-visible:border-action-primary focus-visible:outline focus-visible:outline-2
                `, state.disabled ? "cursor-not-allowed text-tertiary" : `
                      text-secondary
                      hover:text-primary
                    `, addConsumerClasses(state, className));
  }, [className]);
  const renderDefault = React.useCallback(renderProps => {
    const {
      className: clearClassName,
      ...restRenderProps
    } = renderProps;
    return /*#__PURE__*/React.createElement("button", _extends({}, restRenderProps, {
      className: cx("border-0 bg-transparent p-0"),
      type: "button"
    }), /*#__PURE__*/React.createElement(Clear, {
      className: clearClassName,
      "aria-hidden": "true",
      size: "xs"
    }));
  }, []);
  return /*#__PURE__*/React.createElement(BaseUICombobox.ChipRemove, _extends({}, rest, {
    ref: forwardedRef,
    className: getComboboxChipRemoveClassNames,
    render: render ?? renderDefault
  }));
});