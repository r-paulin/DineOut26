function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Combobox.Chips
 * Wraps `Combobox.Input` when `multiple` is enabled so selected values render as removable chips.
 *
 * ```tsx
 * <Combobox.Chips>
 *   <Combobox.Input placeholder="Select cities">
 *     <Combobox.ValueArea>
 *       <Combobox.Value>
 *         {(currentValue) => {
 *           const selected = Array.isArray(currentValue) ? currentValue : [];
 *           return (
 *             <>
 *               {selected.map((option) => (
 *                 <Combobox.Chip key={option.value}>
 *                   {option.label}
 *                   <Combobox.ChipRemove aria-label={`Remove ${option.label}`} />
 *                 </Combobox.Chip>
 *               ))}
 *               <Combobox.Control />
 *             </>
 *           );
 *         }}
 *       </Combobox.Value>
 *     </Combobox.ValueArea>
 *   </Combobox.Input>
 * </Combobox.Chips>
 * ```
 */
export const ComboboxChips = /*#__PURE__*/React.forwardRef(function ComboboxChips(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getComboboxChipsClassNames = React.useCallback(state => cx("w-full min-w-0", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUICombobox.Chips, _extends({}, rest, {
    ref: forwardedRef,
    className: getComboboxChipsClassNames
  }));
});