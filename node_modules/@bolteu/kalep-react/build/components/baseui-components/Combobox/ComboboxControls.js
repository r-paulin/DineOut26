import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useComboboxInputContextSelector } from "./context";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
/**
 * ### Combobox.Controls
 * Flex container for clear buttons, triggers, or custom actions. Place it directly inside `Combobox.Input`.
 *
 * ```tsx
 * <Combobox.Input>
 *   <Combobox.ValueArea>…</Combobox.ValueArea>
 *   <Combobox.Controls>
 *     <Combobox.Clear />
 *     <Combobox.Trigger>
 *       <Combobox.Icon />
 *     </Combobox.Trigger>
 *   </Combobox.Controls>
 * </Combobox.Input>
 * ```
 */
export const ComboboxControls = /*#__PURE__*/React.forwardRef(function ComboboxControls(props, forwardRef) {
  const {
    render,
    className,
    ...otherComponentProps
  } = props;
  const comboboxInputState = useComboboxInputContextSelector(context => context.comboboxInputState);
  const defaultProps = {
    className: cx("flex items-center gap-2", addConsumerClasses(comboboxInputState, className))
  };
  return useRender({
    defaultTagName: "div",
    render,
    ref: forwardRef,
    state: {
      ...comboboxInputState
    },
    props: mergeProps(defaultProps, otherComponentProps)
  });
});