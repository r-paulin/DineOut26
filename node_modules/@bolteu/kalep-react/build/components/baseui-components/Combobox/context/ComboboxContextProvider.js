import * as React from "react";
const ComboboxContext = /*#__PURE__*/React.createContext(null);
export function ComboboxContextProvider({
  children,
  customComboboxProps
}) {
  const inputWrapperRef = React.useRef(null);
  const contextValue = React.useMemo(() => ({
    customComboboxProps,
    inputWrapperRef
  }), [customComboboxProps]);
  return /*#__PURE__*/React.createElement(ComboboxContext.Provider, {
    value: contextValue
  }, children);
}
export function useComboboxContext() {
  const context = React.useContext(ComboboxContext);
  if (!context) {
    throw new Error("Combobox parts must be rendered within Combobox.Root");
  }
  return context;
}
export function useComboboxContextSelector(selector) {
  const context = useComboboxContext();
  return selector(context);
}