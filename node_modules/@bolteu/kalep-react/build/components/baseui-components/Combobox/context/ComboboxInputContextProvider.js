import * as React from "react";
const ComboboxInputContext = /*#__PURE__*/React.createContext(null);
export function ComboboxInputContextProvider({
  children,
  comboboxInputProps,
  comboboxInputState,
  inputRef
}) {
  const contextValue = React.useMemo(() => ({
    comboboxInputProps,
    comboboxInputState,
    inputRef
  }), [comboboxInputProps, comboboxInputState, inputRef]);
  return /*#__PURE__*/React.createElement(ComboboxInputContext.Provider, {
    value: contextValue
  }, children);
}
export function useComboboxInputContext() {
  const context = React.useContext(ComboboxInputContext);
  if (!context) {
    throw new Error("Combobox parts must be rendered within Combobox.Input");
  }
  return context;
}

// TODO: implement deep readonly support

export function useComboboxInputContextSelector(selector) {
  const context = useComboboxInputContext();
  return selector(context);
}