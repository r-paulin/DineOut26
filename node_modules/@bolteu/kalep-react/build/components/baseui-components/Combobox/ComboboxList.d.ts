import * as React from "react";
/**
 * ### Combobox.List
 * Scrollable container for option rows inside `Combobox.Popup`. Provide a function child to render each
 * item from the underlying collection.
 *
 * ```tsx
 * <Combobox.Portal>
 *   <Combobox.Positioner>
 *     <Combobox.Popup>
 *       <Combobox.List>
 *         {(item) => (
 *           <Combobox.Item key={item.value} value={item}>
 *             <span>{item.label}</span>
 *             <Combobox.ItemIndicator />
 *           </Combobox.Item>
 *         )}
 *       </Combobox.List>
 *     </Combobox.Popup>
 *   </Combobox.Positioner>
 * </Combobox.Portal>
 * ```
 */
export declare const ComboboxList: React.ForwardRefExoticComponent<Omit<import("@base-ui/react/combobox").ComboboxListProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=ComboboxList.d.ts.map