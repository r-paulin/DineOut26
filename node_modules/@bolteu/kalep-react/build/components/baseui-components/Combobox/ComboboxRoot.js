import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import { ComboboxContextProvider } from "./context/ComboboxContextProvider";
/**
 * ### Combobox.Root
 * Top-level provider for every other combobox primitive. Define sizing, placeholder, `multiple`, and
 * value props here so the rest of the composition stays in sync.
 *
 * ```tsx
 * <Combobox.Root items={cities} size="md">
 *   <Combobox.Input placeholder="Select a city">
 *     <Combobox.ValueArea>
 *       <Combobox.Control />
 *     </Combobox.ValueArea>
 *     <Combobox.Controls>
 *       <Combobox.Clear />
 *       <Combobox.Trigger>
 *         <Combobox.Icon />
 *       </Combobox.Trigger>
 *     </Combobox.Controls>
 *   </Combobox.Input>
 *   <Combobox.Portal>
 *     <Combobox.Positioner sideOffset={4}>
 *       <Combobox.Popup>
 *         <Combobox.Status>
 *           {statusMessage && <div>{statusMessage}</div>}
 *         </Combobox.Status>
 *         <Combobox.Empty>No cities found</Combobox.Empty>
 *         <Combobox.List>
 *           {(item) => (
 *             <Combobox.Item key={item.value} value={item}>
 *               <span>{item.label}</span>
 *               <Combobox.ItemIndicator />
 *             </Combobox.Item>
 *           )}
 *         </Combobox.List>
 *       </Combobox.Popup>
 *     </Combobox.Positioner>
 *   </Combobox.Portal>
 * </Combobox.Root>
 * ```
 */
export function ComboboxRoot(props) {
  const {
    size = "md",
    multiline = false,
    placeholder,
    children,
    ...rest
  } = props;
  const customComboboxProps = React.useMemo(() => ({
    size,
    multiline,
    placeholder,
    multiple: props.multiple ?? false
  }), [size, multiline, placeholder, props.multiple]);
  return /*#__PURE__*/React.createElement(BaseUICombobox.Root, rest, /*#__PURE__*/React.createElement(ComboboxContextProvider, {
    customComboboxProps: customComboboxProps
  }, children));
}