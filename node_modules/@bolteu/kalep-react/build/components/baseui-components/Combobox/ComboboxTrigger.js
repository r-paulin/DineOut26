function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useComboboxInputContextSelector } from "./context";

/**
 * ### Combobox.Trigger
 * Button used to toggle the popup, typically wrapping `Combobox.Icon`.
 *
 * ```tsx
 * <Combobox.Controls>
 *   <Combobox.Trigger>
 *     <Combobox.Icon />
 *   </Combobox.Trigger>
 * </Combobox.Controls>
 * ```
 */
export const ComboboxTrigger = /*#__PURE__*/React.forwardRef(function ComboboxTrigger(props, forwardedRef) {
  const {
    className,
    children,
    render,
    ...rest
  } = props;
  const readOnly = useComboboxInputContextSelector(context => context.comboboxInputProps?.readOnly ?? false);
  const defaultRender = React.useCallback((triggerProps, state) => {
    const comboboxTriggerClassNames = cx("flex items-center border-0 bg-transparent p-0", state.disabled ? "pointer-events-none cursor-not-allowed" : readOnly ? "cursor-default" : "cursor-pointer", addConsumerClasses(state, className));
    return /*#__PURE__*/React.createElement("button", _extends({}, triggerProps, {
      "aria-hidden": readOnly,
      className: comboboxTriggerClassNames,
      ref: triggerProps.ref,
      type: "button"
    }), children);
  }, [children, className, readOnly]);
  return /*#__PURE__*/React.createElement(BaseUICombobox.Trigger, _extends({}, rest, {
    render: render ?? defaultRender,
    ref: forwardedRef
  }), children);
});