function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import Check from "@bolteu/kalep-react-icons/dist/Check";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Combobox.ItemIndicator
 * Selection icon displayed inside `Combobox.Item`.
 *
 * ```tsx
 * <Combobox.Item value={option}>
 *   {option.label}
 *   <Combobox.ItemIndicator />
 * </Combobox.Item>
 * ```
 */
export const ComboboxItemIndicator = /*#__PURE__*/React.forwardRef(function ComboboxItemIndicator(props, forwardedRef) {
  const {
    className,
    render,
    ...rest
  } = props;
  const getComboboxItemIndicatorClassNames = React.useCallback(state => cx("flex flex-none items-center justify-center text-action-secondary", addConsumerClasses(state, className)), [className]);
  const renderDefault = React.useCallback(props => {
    // ref is not forwarded to the Kalep icons
    const {
      ref: _ref,
      ...rest
    } = props;
    return /*#__PURE__*/React.createElement(Check, _extends({
      "aria-hidden": "true",
      size: "md"
    }, rest));
  }, []);
  return /*#__PURE__*/React.createElement(BaseUICombobox.ItemIndicator, _extends({}, rest, {
    ref: forwardedRef,
    className: getComboboxItemIndicatorClassNames,
    render: render ?? renderDefault
  }));
});