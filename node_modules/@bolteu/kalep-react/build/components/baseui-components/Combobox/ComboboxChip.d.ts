import * as React from "react";
/**
 * ### Combobox.Chip
 * Visual pill that represents a selected value inside `Combobox.Value`.
 *
 * ```tsx
 * <Combobox.Value>
 *   {(currentValue) => {
 *     const selected = Array.isArray(currentValue) ? currentValue : [];
 *     return (
 *       <>
 *         {selected.map((option) => (
 *           <Combobox.Chip key={option.value}>
 *             {option.label}
 *             <Combobox.ChipRemove aria-label={`Remove ${option.label}`} />
 *           </Combobox.Chip>
 *         ))}
 *         <Combobox.Control />
 *       </>
 *     );
 *   }}
 * </Combobox.Value>
 * ```
 */
export declare const ComboboxChip: React.ForwardRefExoticComponent<Omit<import("@base-ui/react/combobox").ComboboxChipProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=ComboboxChip.d.ts.map