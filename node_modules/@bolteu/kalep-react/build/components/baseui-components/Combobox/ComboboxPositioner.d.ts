import * as React from "react";
/**
 * ### Combobox.Positioner
 * Anchors `Combobox.Popup` relative to the input wrapper or a custom anchor (like the chips container).
 *
 * ```tsx
 * <Combobox.Portal>
 *   <Combobox.Positioner anchor={chipsRef} sideOffset={4}>
 *     <Combobox.Popup>…</Combobox.Popup>
 *   </Combobox.Positioner>
 * </Combobox.Portal>
 * ```
 *
 * Override `anchor` when chips should control placement; otherwise it falls back to the input wrapper.
 */
export declare const ComboboxPositioner: React.ForwardRefExoticComponent<Omit<import("@base-ui/react").AutocompletePositionerProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=ComboboxPositioner.d.ts.map