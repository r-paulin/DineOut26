import * as React from "react";
/**
 * ### Combobox.Chips
 * Wraps `Combobox.Input` when `multiple` is enabled so selected values render as removable chips.
 *
 * ```tsx
 * <Combobox.Chips>
 *   <Combobox.Input placeholder="Select cities">
 *     <Combobox.ValueArea>
 *       <Combobox.Value>
 *         {(currentValue) => {
 *           const selected = Array.isArray(currentValue) ? currentValue : [];
 *           return (
 *             <>
 *               {selected.map((option) => (
 *                 <Combobox.Chip key={option.value}>
 *                   {option.label}
 *                   <Combobox.ChipRemove aria-label={`Remove ${option.label}`} />
 *                 </Combobox.Chip>
 *               ))}
 *               <Combobox.Control />
 *             </>
 *           );
 *         }}
 *       </Combobox.Value>
 *     </Combobox.ValueArea>
 *   </Combobox.Input>
 * </Combobox.Chips>
 * ```
 */
export declare const ComboboxChips: React.ForwardRefExoticComponent<Omit<import("@base-ui/react/combobox").ComboboxChipsProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=ComboboxChips.d.ts.map