function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useComboboxPositionerContextSelector } from "./context";

/**
 * ### Combobox.Empty
 * Placeholder content shown when the list has zero items.
 *
 * ```tsx
 * <Combobox.Popup>
 *   <Combobox.Empty>No cities found</Combobox.Empty>
 *   <Combobox.List>…</Combobox.List>
 * </Combobox.Popup>
 * ```
 *
 * Combine with actions or hints to guide the user toward the next step.
 */
export const ComboboxEmpty = /*#__PURE__*/React.forwardRef(function ComboboxEmpty(props, forwardedRef) {
  const {
    className,
    children,
    ...rest
  } = props;
  const empty = useComboboxPositionerContextSelector(context => context.comboboxPositionerState?.empty);
  const getComboboxEmptyClassNames = React.useCallback(state => cx(`flex items-center px-6 py-2 text-primary`, addConsumerClasses(state, className)), [className]);
  return empty && /*#__PURE__*/React.createElement(BaseUICombobox.Empty, _extends({}, rest, {
    ref: forwardedRef,
    className: getComboboxEmptyClassNames
  }), children);
});