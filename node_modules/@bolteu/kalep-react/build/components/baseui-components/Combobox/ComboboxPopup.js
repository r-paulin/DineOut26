function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Combobox as BaseUICombobox } from "@base-ui/react/combobox";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Combobox.Popup
 * Surface that houses `Combobox.List`, `Combobox.Empty`, or custom footers.
 *
 * ```tsx
 * <Combobox.Positioner>
 *   <Combobox.Popup>
 *     <Combobox.Empty>No options found</Combobox.Empty>
 *     <Combobox.List>…</Combobox.List>
 *   </Combobox.Popup>
 * </Combobox.Positioner>
 * ```
 */
export const ComboboxPopup = /*#__PURE__*/React.forwardRef(function ComboboxPopup(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getComboboxPopupClassNames = React.useCallback(state => cx(`
                  max-h-80 min-w-[var(--anchor-width)] max-w-[var(--anchor-width)] overflow-hidden rounded-md
                  bg-layer-floor-3 shadow-md
                `, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUICombobox.Popup, _extends({}, rest, {
    ref: forwardedRef,
    className: getComboboxPopupClassNames
  }));
});