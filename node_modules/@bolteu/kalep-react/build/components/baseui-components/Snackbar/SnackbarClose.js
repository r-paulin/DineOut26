function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import Cross from "@bolteu/kalep-react-icons/dist/Cross";
import { IconButton } from "../IconButton";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Snackbar.Close
 * Dismiss button rendered inside the snackbar.
 *
 * ```tsx
 * <Snackbar.Close aria-label="Close" />
 * ```
 */
export const SnackbarClose = /*#__PURE__*/React.forwardRef(function SnackbarClose(props, forwardedRef) {
  const {
    className,
    render,
    "aria-label": ariaLabel,
    ...rest
  } = props;
  const getCloseClassNames = React.useCallback(state => cx("inline-flex h-5 w-5 items-center justify-center", "text-primary-inverted", "hover:opacity-80", addConsumerClasses(state, className)), [className]);
  const renderDefault = React.useCallback(iconProps => {
    const {
      children: _children,
      ...restIconProps
    } = iconProps;
    return /*#__PURE__*/React.createElement(IconButton, _extends({}, restIconProps, {
      "aria-label": ariaLabel ?? "Close",
      icon: /*#__PURE__*/React.createElement(Cross, {
        size: "sm"
      })
    }));
  }, [ariaLabel]);
  return /*#__PURE__*/React.createElement(BaseUIToast.Close, _extends({}, rest, {
    "aria-label": ariaLabel ?? "Close",
    className: getCloseClassNames,
    render: render ?? renderDefault,
    ref: forwardedRef
  }));
});