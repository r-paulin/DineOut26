function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Snackbar.Title
 * Optional title for the snackbar message.
 *
 * ```tsx
 * <Snackbar.Title>Update complete</Snackbar.Title>
 * ```
 */
export const SnackbarTitle = /*#__PURE__*/React.forwardRef(function SnackbarTitle(props, forwardedRef) {
  const {
    className,
    render,
    ...rest
  } = props;
  const getTitleClassNames = React.useCallback(state => cx("m-0 font-semibold", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIToast.Title, _extends({}, rest, {
    className: getTitleClassNames,
    ref: forwardedRef
    // by default it's rendered as h2 element, but we want to render it as p for snackbar
    ,
    render: render ?? /*#__PURE__*/React.createElement("p", null)
  }));
});