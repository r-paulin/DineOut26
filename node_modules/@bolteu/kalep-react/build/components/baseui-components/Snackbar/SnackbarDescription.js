function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Snackbar.Description
 * Main text content for a snackbar.
 *
 * ```tsx
 * <Snackbar.Description>Changes saved successfully.</Snackbar.Description>
 * ```
 */
export const SnackbarDescription = /*#__PURE__*/React.forwardRef(function SnackbarDescription(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getDescriptionClassNames = React.useCallback(state => cx("m-0 min-w-0 flex-1", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIToast.Description, _extends({}, rest, {
    className: getDescriptionClassNames,
    ref: forwardedRef
  }));
});