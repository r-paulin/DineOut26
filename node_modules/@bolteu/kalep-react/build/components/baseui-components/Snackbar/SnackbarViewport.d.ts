import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import { type SnackbarPlacement } from "./context";
export interface SnackbarViewportProps extends BaseUIToast.Viewport.Props {
    placement?: SnackbarPlacement;
}
/**
 * ### Snackbar.Viewport
 * Container that positions the snackbar stack.
 *
 * ```tsx
 * <Snackbar.Viewport placement="bottom-left" />
 * ```
 */
export declare const SnackbarViewport: React.ForwardRefExoticComponent<Omit<SnackbarViewportProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=SnackbarViewport.d.ts.map