function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Snackbar.Action
 * Optional action button for the snackbar.
 *
 * ```tsx
 * <Snackbar.Action>Undo</Snackbar.Action>
 * ```
 */
export const SnackbarAction = /*#__PURE__*/React.forwardRef(function SnackbarAction(props, forwardedRef) {
  const {
    className,
    render,
    ...rest
  } = props;
  const getActionClassNames = React.useCallback(state => cx("text-action-primary-inverted", "bolt-font-body-s-accent inline-block cursor-pointer border-0 bg-transparent p-0", addConsumerClasses(state, className)), [className]);
  const renderDefault = React.useCallback(iconProps => {
    const {
      children: _children,
      ...restIconProps
    } = iconProps;
    return /*#__PURE__*/React.createElement("button", restIconProps, iconProps.children);
  }, []);
  return /*#__PURE__*/React.createElement(BaseUIToast.Action, _extends({}, rest, {
    className: getActionClassNames,
    ref: forwardedRef,
    render: render ?? renderDefault
  }));
});