function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { SnackbarViewportContextProvider } from "./context";
const CLASSNAME_TW_SNACKBAR_VIEWPORT_PLACEMENT = {
  "bottom-left": "bottom-4 left-4 items-start",
  "bottom-right": "bottom-4 right-4 items-end",
  "bottom-center": "bottom-4 left-1/2 -translate-x-1/2 items-center",
  "top-left": "left-4 top-4 items-start",
  "top-right": "right-4 top-4 items-end",
  "top-center": "left-1/2 top-4 -translate-x-1/2 items-center"
};

/**
 * ### Snackbar.Viewport
 * Container that positions the snackbar stack.
 *
 * ```tsx
 * <Snackbar.Viewport placement="bottom-left" />
 * ```
 */
export const SnackbarViewport = /*#__PURE__*/React.forwardRef(function SnackbarViewport(props, forwardedRef) {
  const {
    className,
    placement = "bottom-left",
    ...rest
  } = props;
  const getViewportClassNames = React.useCallback(state => cx(`
                  fixed z-50 flex w-[calc(100%-2rem)] max-w-sm gap-2 outline-none
                  [&:empty]:pointer-events-none
                `, placement.startsWith("top") ? "flex-col" : "flex-col-reverse", CLASSNAME_TW_SNACKBAR_VIEWPORT_PLACEMENT[placement], addConsumerClasses(state, className)), [className, placement]);
  return /*#__PURE__*/React.createElement(SnackbarViewportContextProvider, {
    placement: placement
  }, /*#__PURE__*/React.createElement(BaseUIToast.Viewport, _extends({}, rest, {
    className: getViewportClassNames,
    ref: forwardedRef
  })));
});