function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Toast as BaseUIToast } from "@base-ui/react/toast";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Snackbar.Content
 * Layout wrapper for snackbar body, title, and actions.
 *
 * ```tsx
 * <Snackbar.Content>
 *   <div>
 *     <Snackbar.Title />
 *     <Snackbar.Description />
 *   </div>
 * </Snackbar.Content>
 * ```
 */
export const SnackbarContent = /*#__PURE__*/React.forwardRef(function SnackbarContent(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getContentClassNames = React.useCallback(state => cx(`
                  flex w-full flex-row flex-wrap items-center gap-3 overflow-hidden transition-opacity duration-300
                  ease-in-out
                `, {
    "opacity-0": state.behind,
    "opacity-100": state.expanded
  }, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIToast.Content, _extends({}, rest, {
    className: getContentClassNames,
    ref: forwardedRef
  }));
});