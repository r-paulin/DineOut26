function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Field as BaseUIField } from "@base-ui/react/field";
import { addConsumerClasses } from "../../utils/combine-classes";
import { useId } from "../../../../helpers";
import { cx } from "../../../../helpers/utils/cx";
export const Root = /*#__PURE__*/React.forwardRef(function Field(props, ref) {
  const {
    className,
    id,
    ...rest
  } = props;
  const generatedId = useId();
  const inputId = id ?? `kalep-field-${generatedId}`;
  const fieldRootClassName = React.useCallback(state => {
    return cx("flex w-full flex-col items-start gap-2", addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUIField.Root, _extends({
    id: inputId
  }, rest, {
    className: fieldRootClassName,
    ref: ref
  }));
});