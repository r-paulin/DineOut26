function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Slider as BaseUISlider } from "@base-ui/react/slider";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Slider.Indicator
 * Highlights the active portion of the track (filled area).
 *
 * ```tsx
 * <Slider.Track>
 *   <Slider.Indicator />
 * </Slider.Track>
 * ```
 */
export const SliderIndicator = /*#__PURE__*/React.forwardRef(function SliderIndicator(props, ref) {
  const {
    className,
    ...rest
  } = props;
  const sliderIndicatorClassName = React.useCallback(state => {
    const invalid = state.valid === false;
    return cx("pointer-events-none rounded-full bg-action-primary", "h-full", "transition-colors duration-150 ease-linear", {
      "bg-neutral-secondary": state.disabled && !invalid,
      "bg-danger-primary": invalid
    }, addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUISlider.Indicator, _extends({
    ref: ref,
    className: sliderIndicatorClassName
  }, rest));
});