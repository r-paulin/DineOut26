function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Slider as BaseUISlider } from "@base-ui/react/slider";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Slider.Track
 * Visual baseline of the control, typically wraps `Slider.Indicator`.
 *
 * ```tsx
 * <Slider.Track>
 *   <Slider.Indicator />
 * </Slider.Track>
 * ```
 */
export const SliderTrack = /*#__PURE__*/React.forwardRef(function SliderTrack(props, ref) {
  const {
    className,
    ...rest
  } = props;
  const sliderTrackClassName = React.useCallback(state => {
    const invalid = state.valid === false;
    return cx("relative h-1 w-full rounded-full bg-neutral-secondary-hard", "transition-colors duration-150 ease-linear", {
      "bg-special-disabled": state.disabled && !invalid,
      "bg-danger-secondary": invalid
    }, addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUISlider.Track, _extends({
    ref: ref,
    className: sliderTrackClassName
  }, rest));
});