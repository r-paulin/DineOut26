function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Slider as BaseUISlider } from "@base-ui/react/slider";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Slider.Root
 * Provides context for all slider components.
 *
 * ```tsx
 * <Slider.Root defaultValue={35}>
 *   <Slider.Control>
 *     <Slider.Track>
 *       <Slider.Indicator />
 *       <Slider.Thumb />
 *     </Slider.Track>
 *   </Slider.Control>
 *   <Slider.Value />
 * </Slider.Root>
 * ```
 */
export function SliderRoot(props) {
  const {
    className,
    children,
    ...rest
  } = props;
  const sliderRootClassName = React.useCallback(state => {
    return cx("flex w-full flex-col gap-3", addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUISlider.Root, _extends({}, rest, {
    className: sliderRootClassName
  }), children);
}