function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Slider as BaseUISlider } from "@base-ui/react/slider";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Slider.Value
 * Renders the current slider value(s) as text or custom JSX.
 *
 * ```tsx
 * <Slider.Value>{(formatted) => formatted.join(" / ")}</Slider.Value>
 * ```
 */
export const SliderValue = /*#__PURE__*/React.forwardRef(function SliderValue(props, ref) {
  const {
    className,
    ...rest
  } = props;
  const sliderValueClassName = React.useCallback(state => {
    return cx("bolt-font-body-s-regular text-secondary", "inline-flex items-center", addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUISlider.Value, _extends({
    ref: ref,
    className: sliderValueClassName
  }, rest));
});