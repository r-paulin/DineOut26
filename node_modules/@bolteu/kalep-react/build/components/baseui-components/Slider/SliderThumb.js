function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Slider as BaseUISlider } from "@base-ui/react/slider";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Slider.Thumb
 * Draggable handle that sets the slider value.
 *
 * ```tsx
 * <Slider.Control>
 *   <Slider.Track>
 *     <Slider.Indicator />
 *     <Slider.Thumb />
 *   </Slider.Track>
 * </Slider.Control>
 * ```
 */
export const SliderThumb = /*#__PURE__*/React.forwardRef(function SliderThumb(props, ref) {
  const {
    className,
    children,
    ...rest
  } = props;
  const sliderThumbClassName = React.useCallback(state => {
    const invalid = state.valid === false;
    return cx("h-6 w-6 border-4", "box-border rounded-full border-solid border-action-primary bg-static-key-light shadow-md", `
                  has-[:focus-visible]:outline has-[:focus-visible]:outline-1 has-[:focus-visible]:outline-offset-1
                  has-[:focus-visible]:[outline-color:var(--color-bg-action-primary)]
                `, state.disabled ? "cursor-not-allowed" : `
                      cursor-grab
                      active:cursor-grabbing
                    `, {
      "border-neutral-secondary shadow-none": state.disabled && !invalid,
      "border-danger-primary": invalid,
      "shadow-lg": state.dragging && !state.disabled
    }, addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUISlider.Thumb, _extends({
    ref: ref,
    className: sliderThumbClassName
  }, rest), children);
});