function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Slider as BaseUISlider } from "@base-ui/react/slider";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Slider.Control
 * Click / drag surface that hosts the track + thumbs.
 *
 * ```tsx
 * <Slider.Control>
 *   <Slider.Track>
 *     <Slider.Indicator />
 *     <Slider.Thumb />
 *   </Slider.Track>  
 * </Slider.Control>
 * ```
 */
export const SliderControl = /*#__PURE__*/React.forwardRef(function SliderControl(props, ref) {
  const {
    className,
    ...rest
  } = props;
  const sliderControlClassName = React.useCallback(state => {
    return cx("relative flex w-full items-center py-2", "touch-none select-none", state.disabled ? "cursor-not-allowed" : "cursor-pointer", addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUISlider.Control, _extends({
    ref: ref,
    className: sliderControlClassName
  }, rest));
});