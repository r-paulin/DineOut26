import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useListItemLayoutContext } from "./context/useListItemLayoutContext";
export const ListItemLayoutSlot = /*#__PURE__*/React.forwardRef(function ListItemLayoutSlot(props, ref) {
  const {
    render,
    className,
    children,
    ...otherComponentProps
  } = props;
  const state = useListItemLayoutContext();
  const defaultProps = {
    className: cx("flex h-full shrink-0 items-center", addConsumerClasses(state, className)),
    children
  };
  return useRender({
    defaultTagName: "div",
    render,
    ref,
    state,
    props: mergeProps(defaultProps, otherComponentProps)
  });
});