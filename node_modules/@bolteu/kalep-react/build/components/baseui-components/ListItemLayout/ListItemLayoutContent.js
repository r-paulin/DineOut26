import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useListItemLayoutContext } from "./context/useListItemLayoutContext";
export const ListItemLayoutContent = /*#__PURE__*/React.forwardRef(function ListItemLayoutContent(props, ref) {
  const {
    render,
    className,
    ...otherComponentProps
  } = props;
  const state = useListItemLayoutContext();
  const defaultProps = {
    className: cx("flex-1", addConsumerClasses(state, className))
  };
  return useRender({
    defaultTagName: "div",
    render,
    ref,
    state,
    props: mergeProps(defaultProps, otherComponentProps)
  });
});