function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Menu as BaseUIMenu } from "@base-ui/react/menu";
import { ListItemLayout } from "../ListItemLayout";
import { getItemDefaultClassNames } from "./shared-styles";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
const DEFAULT_DELAY = 0;
const DEFAULT_CLOSE_DELAY = 0;

/**
 * ### Menu.SubmenuTrigger
 * Menu item that opens a nested submenu.
 * Renders a ListItemLayout.Root component internally, so you can use ListItemLayout slots if needed.
 *
 * Regular usage:
 * ```tsx
 * <Menu.SubmenuTrigger>More</Menu.SubmenuTrigger>
 * ```
 *
 * Composed with slots:
 * ```tsx
 * <Menu.SubmenuTrigger>
 *   <ListItemLayout.Slot>
 *     <Icon size="md" />
 *   </ListItemLayout.Slot>
 *   <ListItemLayout.Content>
 *     <TypographyStack>
 *       <TypographyStack.Primary>More</TypographyStack.Primary>
 *       <TypographyStack.Secondary>Additional options</TypographyStack.Secondary>
 *     </TypographyStack>
 *   </ListItemLayout.Content>
 *   <ListItemLayout.Slot>
 *     <ChevronRight size="md" />
 *   </ListItemLayout.Slot>
 * </Menu.SubmenuTrigger>
 * ```
 */
export const MenuSubmenuTrigger = /*#__PURE__*/React.forwardRef(function MenuSubmenuTrigger(props, forwardedRef) {
  const {
    className,
    delay = DEFAULT_DELAY,
    closeDelay = DEFAULT_CLOSE_DELAY,
    children,
    render,
    ...rest
  } = props;
  const getSubmenuTriggerClassNames = React.useCallback(state => cx(getItemDefaultClassNames(state), addConsumerClasses(state, className)), [className]);
  const defaultRender = React.useCallback((triggerProps, state) => {
    return /*#__PURE__*/React.createElement(ListItemLayout.Root, _extends({}, triggerProps, {
      disabled: state.disabled
    }), children);
  }, [children]);
  return /*#__PURE__*/React.createElement(BaseUIMenu.SubmenuTrigger, _extends({}, rest, {
    delay: delay,
    closeDelay: closeDelay,
    render: render ?? defaultRender,
    className: getSubmenuTriggerClassNames,
    ref: forwardedRef
  }), children);
});