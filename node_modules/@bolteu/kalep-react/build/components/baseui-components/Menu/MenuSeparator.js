function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Menu as BaseUIMenu } from "@base-ui/react/menu";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Menu.Separator
 * Visual divider between groups of menu items.
 *
 * ```tsx
 * <Menu.Separator />
 * ```
 */
export const MenuSeparator = /*#__PURE__*/React.forwardRef(function MenuSeparator(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getSeparatorClassNames = React.useCallback(state => cx("h-px bg-neutral-secondary", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIMenu.Separator, _extends({}, rest, {
    className: getSeparatorClassNames,
    ref: forwardedRef
  }));
});