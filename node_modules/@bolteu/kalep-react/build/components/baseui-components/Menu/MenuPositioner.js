function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Menu as BaseUIMenu } from "@base-ui/react/menu";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Menu.Positioner
 * Positions the menu popup relative to the trigger.
 *
 * ```tsx
 * <Menu.Positioner>
 *   <Menu.Popup />
 * </Menu.Positioner>
 * ```
 */

const DEFAULT_SIDE_OFFSET = 4;
export const MenuPositioner = /*#__PURE__*/React.forwardRef(function MenuPositioner(props, forwardedRef) {
  const {
    className,
    align = "start",
    sideOffset = DEFAULT_SIDE_OFFSET,
    ...rest
  } = props;
  const getPositionerClassNames = React.useCallback(state => cx("outline-none", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIMenu.Positioner, _extends({
    sideOffset: sideOffset,
    align: align
  }, rest, {
    className: getPositionerClassNames,
    ref: forwardedRef
  }));
});