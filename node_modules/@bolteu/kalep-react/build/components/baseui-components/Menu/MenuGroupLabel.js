function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Menu as BaseUIMenu } from "@base-ui/react/menu";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Menu.GroupLabel
 * Non-interactive label for grouped menu items.
 *
 * ```tsx
 * <Menu.GroupLabel>Actions</Menu.GroupLabel>
 * ```
 */
export const MenuGroupLabel = /*#__PURE__*/React.forwardRef(function MenuGroupLabel(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getGroupLabelClassNames = React.useCallback(state => cx("bolt-font-body-s-regular cursor-default px-4 py-2 text-secondary", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIMenu.GroupLabel, _extends({}, rest, {
    className: getGroupLabelClassNames,
    ref: forwardedRef
  }));
});