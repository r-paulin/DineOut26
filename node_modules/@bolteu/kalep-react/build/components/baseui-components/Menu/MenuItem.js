function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Menu as BaseUIMenu } from "@base-ui/react/menu";
import { ListItemLayout } from "../ListItemLayout";
import { getItemDefaultClassNames } from "./shared-styles";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Menu.Item
 * Single action row rendered inside `Menu.Popup`.
 * Renders a ListItemLayout.Root component internally, so you can use ListItemLayout slots if needed.
 *
 * Regular usage:
 * ```tsx
 * <Menu.Item>Profile</Menu.Item>
 * ```
 *
 * Composed with slots:
 * ```tsx
 * <Menu.Item>
 *   <ListItemLayout.Slot>
 *     <Icon size="md" />
 *   </ListItemLayout.Slot>
 *   <ListItemLayout.Content>
 *     <TypographyStack>
 *       <TypographyStack.Primary>Profile</TypographyStack.Primary>
 *       <TypographyStack.Secondary>View and edit your profile</TypographyStack.Secondary>
 *     </TypographyStack>
 *   </ListItemLayout.Content>
 *   <ListItemLayout.Slot>
 *     <ChevronRight size="md" />
 *   </ListItemLayout.Slot>
 * </Menu.Item>
 * ```
 */
export const MenuItem = /*#__PURE__*/React.forwardRef(function MenuItem(props, forwardedRef) {
  const {
    className,
    children,
    render,
    ...rest
  } = props;
  const getItemClassNames = React.useCallback(state => cx(getItemDefaultClassNames(state), addConsumerClasses(state, className)), [className]);
  const defaultRender = React.useCallback((itemProps, state) => {
    return /*#__PURE__*/React.createElement(ListItemLayout.Root, _extends({}, itemProps, {
      disabled: state.disabled
    }), children);
  }, [children]);
  return /*#__PURE__*/React.createElement(BaseUIMenu.Item, _extends({}, rest, {
    ref: forwardedRef,
    render: render ?? defaultRender,
    className: getItemClassNames
  }), children);
});