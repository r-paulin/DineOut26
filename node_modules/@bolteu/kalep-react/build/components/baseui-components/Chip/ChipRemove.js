import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import Cross from "@bolteu/kalep-react-icons/dist/Cross";
import { useChipContext } from "./ChipContext";
import { cx } from "../../../helpers/utils/cx";
import { withSize } from "./Chip.utils";
export const ChipRemove = /*#__PURE__*/React.forwardRef(function ChipRemove(props, ref) {
  const {
    className,
    render,
    ...otherComponentProps
  } = props;
  const context = useChipContext();
  const removeHandler = context?.onRemove;
  const disabled = context?.disabled ?? false;
  const onRemove = React.useCallback(event => {
    if (disabled) return;
    // So that the click doesn't trigger the Chip's onClick
    event.stopPropagation();
    removeHandler?.(event);
  }, [disabled, removeHandler]);
  const defaultProps = {
    onClick: onRemove,
    role: "button",
    "aria-hidden": "true",
    className: cx("-mx-2 flex h-full cursor-pointer items-center pe-1 ps-2", className, {
      "pointer-events-none cursor-not-allowed": disabled
    }),
    children: withSize(/*#__PURE__*/React.createElement(Cross, null), context?.size ?? "md")
  };
  return useRender({
    defaultTagName: "span",
    render,
    ref,
    state: {},
    props: mergeProps(defaultProps, otherComponentProps)
  });
});