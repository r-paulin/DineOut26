import * as React from "react";
import type { ChipProps } from "./Chip.types";
/**
 * ### Composition guide
 * Regular usage:
 * ```tsx
 * <Chip>
 *   Label
 * </Chip>
 * ```
 *
 * With icon and remove action:
 * ```tsx
 * <Chip onRemove={handleRemove}>
 *   <Chip.Icon icon={<Lock size="sm" />} />
 *   Secure
 *   <Chip.Remove />
 * </Chip>
 * ```
 *
 * Include `Chip.Remove` when providing `onRemove` so the remove affordance is visible.
 */
export declare const ChipRoot: React.ForwardRefExoticComponent<Omit<ChipProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
//# sourceMappingURL=ChipRoot.d.ts.map