import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { SpinnerIcon } from "../../_internal/SpinnerIcon";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { CLASSNAME_TW_SPINNER_SIZE_MAP } from "./spinner.types";
/**
 * ### Spinner
 * Basic indeterminate loader.
 *
 * ```tsx
 * <Spinner />
 * ```
 */
export const Spinner = /*#__PURE__*/React.forwardRef(function Spinner(props, ref) {
  const {
    size = 500,
    className,
    render,
    ...restProps
  } = props;
  const state = React.useMemo(() => ({
    size
  }), [size]);
  const sizeClassName = CLASSNAME_TW_SPINNER_SIZE_MAP[size];
  const icon = /*#__PURE__*/React.createElement(SpinnerIcon, {
    "aria-hidden": "true",
    size: "inherit",
    className: cx("inline-block animate-spin")
  });
  const defaultProps = {
    role: "status",
    "aria-live": "polite",
    className: cx("inline-flex items-center justify-center text-action-primary", sizeClassName, addConsumerClasses(state, className)),
    children: icon
  };
  return useRender({
    defaultTagName: "span",
    render,
    ref,
    state,
    props: mergeProps(defaultProps, restProps)
  });
});