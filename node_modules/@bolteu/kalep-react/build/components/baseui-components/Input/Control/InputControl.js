import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useInput } from "../context/useInput";
import { addConsumerClasses } from "../../utils/combine-classes";
import { cx } from "../../../../helpers/utils/cx";
export const InputControl = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
  const {
    render,
    className,
    ...otherProps
  } = props;
  const context = useInput();
  const {
    inputProps,
    inputState
  } = context;
  const {
    ref: refFromContext,
    className: _ctxCn,
    // Removing props from parent, that will not be passed to the input element
    ...ctxProps
  } = inputProps;
  const inputControlState = {
    ...inputState
  };
  const defaultProps = {
    className: cx(`
                  m-0 h-full w-full min-w-0 flex-grow border-0 border-solid bg-transparent p-0 text-size-inherit
                  outline-none
                  [color:inherit]
                  input-clear-button:hidden
                `, inputControlState.disabled ? "cursor-not-allowed placeholder-tertiary" : "placeholder-secondary", addConsumerClasses(inputControlState, className)),
    ...ctxProps
  };
  return useRender({
    defaultTagName: "input",
    render,
    ref: refFromContext ? [refFromContext, forwardedRef] : forwardedRef,
    state: inputControlState,
    props: mergeProps(defaultProps, otherProps)
  });
});
InputControl.displayName = "Input.Control";