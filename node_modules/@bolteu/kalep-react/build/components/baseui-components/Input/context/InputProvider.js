import * as React from "react";
export const InputContext = /*#__PURE__*/React.createContext(null);
export function InputProvider({
  children,
  inputState,
  inputProps
}) {
  const contextValue = React.useMemo(() => ({
    inputState,
    inputProps
  }), [inputState, inputProps]);
  return /*#__PURE__*/React.createElement(InputContext.Provider, {
    value: contextValue
  }, children);
}