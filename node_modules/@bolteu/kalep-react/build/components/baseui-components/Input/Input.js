function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Input as BaseUIInput, useRender } from "@base-ui/react";
import Alert from "@bolteu/kalep-react-icons/dist/Alert";
import { InputProvider } from "./context/InputProvider";
import { InputControl } from "./Control/InputControl";
import { InputSlot } from "./Slot/InputSlot";
import { addConsumerClasses } from "../utils/combine-classes";
import { useId } from "../../../helpers";
import { cx } from "../../../helpers/utils/cx";
import { mergeRefs } from "../../../helpers/utils/mergeRefs";
const sizes = {
  sm: "h-9 px-2 text-sm gap-2",
  md: "h-12 px-3 text-base gap-3",
  lg: "h-14 px-4 text-base gap-3"
};
const states = {
  idle: "border-special-nulled focus-within:!border-action-primary text-primary",
  disabled: "border-neutral-secondary cursor-not-allowed text-tertiary bg-special-nulled",
  error: "border-danger-primary text-primary caret-danger-primary"
};
function InputInner(props) {
  const {
    state,
    render,
    children,
    fieldSize,
    onClick,
    className,
    consumerRef,
    ...otherProps
  } = props;
  const invalid = state.valid === false;
  const internalInputRef = React.useRef(null);
  const element = useRender({
    defaultTagName: "div",
    render,
    state,
    props: {
      className,
      onClick(ev) {
        if (!props.disabled) {
          if (internalInputRef?.current && document.activeElement !== internalInputRef?.current) {
            internalInputRef.current.focus();
          }
          onClick?.(ev);
        }
      },
      role: "presentation",
      children: /*#__PURE__*/React.createElement(React.Fragment, null, children || /*#__PURE__*/React.createElement(InputControl, null), invalid ? /*#__PURE__*/React.createElement(InputSlot, {
        "data-testid": "input-error-icon",
        "aria-hidden": "true",
        className: cx("flex-none text-danger-secondary"),
        render: /*#__PURE__*/React.createElement(Alert, {
          size: fieldSize === "sm" ? "sm" : "lg"
        })
      }) : null)
    }
  });
  const controlRefWithInternal = mergeRefs([consumerRef, internalInputRef]);
  return /*#__PURE__*/React.createElement(InputProvider, {
    inputProps: {
      ...otherProps,
      ref: controlRefWithInternal
    },
    inputState: state
  }, element);
}
/**
 * ### Composition guide
 * Regular usage:
 *
 * ```tsx
 * <Input />
 * ```
 *
 * Composed with subcomponents:
 * ```tsx
 * <Input>
 *     <Input.Slot>some string slot</Input.Slot>
 *     <Input.Control />
 *     <Input.Slot render={<SomeIcon />} />
 * </Input>
 * ```
 * A `render` prop is provided for the `Input`, `Input.Slot` and `Input.Control` components to
 * allow custom rendering of the components respectively.
 *
 * See `Field` and `Form` component documentation for better understanding of the field composition in a form.
 */
export const InputComponent = /*#__PURE__*/React.forwardRef(function InputWrapper(props, consumerInputRef) {
  const {
    fieldSize = "md",
    render,
    className,
    ...rest
  } = props;
  const generatedId = useId();
  const inputId = props.id ?? `kalep-input-${generatedId}`;
  const inputClassName = React.useCallback(function inputClassname(state) {
    const invalid = state.valid === false;
    return cx("flex w-full items-center justify-end", "rounded-md border-2 border-solid", "bg-neutral-secondary caret-action-primary", "duration-100 ease-linear", "focus-within:bg-special-nulled", state.disabled ? "cursor-not-allowed" : "cursor-text", {
      [states.disabled]: state.disabled,
      [states.error]: invalid,
      [states.idle]: !state.disabled && !invalid
    }, sizes[fieldSize], addConsumerClasses(state, className));
  }, [className, fieldSize]);
  const defaultRender = React.useCallback(function defaultInputRender({
    ref,
    ...renderProps
  }, state) {
    const inputRef = mergeRefs([ref, consumerInputRef]);
    return /*#__PURE__*/React.createElement(InputInner, _extends({
      state: state,
      fieldSize: fieldSize,
      consumerRef: inputRef
    }, renderProps, {
      render: render
    }));
  }, [fieldSize, render, consumerInputRef]);
  return /*#__PURE__*/React.createElement(BaseUIInput, _extends({
    render: defaultRender,
    className: inputClassName,
    id: inputId
  }, rest));
});
InputComponent.displayName = "Input";