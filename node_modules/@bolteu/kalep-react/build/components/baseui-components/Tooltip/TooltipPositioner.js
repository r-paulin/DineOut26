function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Tooltip as BaseUITooltip } from "@base-ui/react/tooltip";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
const DEFAULT_SIDE = "top";
const DEFAULT_SIDE_OFFSET = 6;

/**
 * ### Tooltip.Positioner
 * Handles floating-ui placement around the trigger.
 *
 * ```tsx
 * <Tooltip.Positioner side="bottom">
 *   <Tooltip.Popup>Content</Tooltip.Popup>
 * </Tooltip.Positioner>
 * ```
 */
export const TooltipPositioner = /*#__PURE__*/React.forwardRef(function TooltipPositioner(props, forwardedRef) {
  const {
    className,
    side = DEFAULT_SIDE,
    sideOffset = DEFAULT_SIDE_OFFSET,
    ...rest
  } = props;
  const getPositionerClassNames = React.useCallback(state => cx("pointer-events-none z-tooltip", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUITooltip.Positioner, _extends({}, rest, {
    side: side,
    sideOffset: sideOffset,
    className: getPositionerClassNames,
    ref: forwardedRef
  }));
});