function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Tooltip as BaseUITooltip } from "@base-ui/react/tooltip";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
const DEFAULT_DELAY = 25;
const DEFAULT_CLOSE_DELAY = 0;

/**
 * ### Tooltip.Trigger
 * Wraps the interactive element that opens the tooltip.
 *
 * ```tsx
 * <Tooltip.Trigger aria-label="More info">
 *   <Button>Hover me</Button>
 * </Tooltip.Trigger>
 * ```
 */
export const TooltipTrigger = /*#__PURE__*/React.forwardRef(function TooltipTrigger(props, forwardedRef) {
  const {
    className,
    delay = DEFAULT_DELAY,
    closeDelay = DEFAULT_CLOSE_DELAY,
    ...rest
  } = props;
  const getTriggerClassNames = React.useCallback(state => cx(addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUITooltip.Trigger, _extends({}, rest, {
    delay: delay,
    closeDelay: closeDelay,
    className: getTriggerClassNames,
    ref: forwardedRef
  }));
});