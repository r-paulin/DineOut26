function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Radio as BaseUIRadio } from "@base-ui/react";
import { addConsumerClasses } from "../../utils/combine-classes";
import { cx } from "../../../../helpers/utils/cx";
export const Radio = /*#__PURE__*/React.forwardRef(function Radio(props, ref) {
  const {
    className,
    ...rest
  } = props;
  const radioRootClassname = React.useCallback(state => {
    const {
      disabled,
      valid,
      checked
    } = state;
    const invalid = valid === false;
    return cx("m-0 flex h-6 w-6 shrink-0 rounded-full", "border-2 border-solid bg-special-nulled", "duration-150 ease-linear", disabled ? "cursor-not-allowed border-0 bg-special-disabled" : "cursor-pointer", invalid ? "border-danger-primary" : "border-neutral-primary", {
      "border-[7px]": checked,
      "border-action-primary": checked && !invalid,
      "border-separator bg-special-nulled": disabled && checked,
      "bg-static-key-light": !disabled && checked,
      "hover:border-active-danger-primary": disabled && invalid,
      "hover:border-active-neutral-primary": !disabled && !invalid,
      "hover:border-active-action-primary": !disabled && !invalid && checked
    }, addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUIRadio.Root, _extends({
    className: radioRootClassname
  }, rest, {
    ref: ref
  }));
});