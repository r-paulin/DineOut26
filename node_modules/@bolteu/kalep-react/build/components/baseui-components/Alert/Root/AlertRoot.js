import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useControlled } from "@base-ui/utils/useControlled";
import { colors } from "../consts";
import { AlertContext } from "../context/AlertProvider";
import { addConsumerClasses } from "../../utils/combine-classes";
import { useId } from "../../../../helpers";
import { cx } from "../../../../helpers/utils/cx";
export const AlertRoot = /*#__PURE__*/React.forwardRef(function AlertRoot(props, ref) {
  const {
    severity = "success",
    className,
    visible: visibilityProp,
    defaultVisible = true,
    render,
    id: idProp,
    onVisibleChange: onVisibleChangeProp,
    ...otherComponentProps
  } = props;
  const [visible, setVisible] = useControlled({
    controlled: visibilityProp,
    default: defaultVisible,
    name: "Alert",
    state: "visible"
  });
  const onVisibleChange = React.useCallback(newVisible => {
    onVisibleChangeProp?.(newVisible);
  }, [onVisibleChangeProp]);
  const [descriptionId, setDescriptionId] = React.useState(undefined);
  const [titleId, setTitleId] = React.useState(undefined);
  const generatedId = useId();
  const id = idProp ?? `kalep-alert-${generatedId}`;
  const state = {
    severity,
    visible
  };
  const defaultProps = {
    className: cx("flex w-full gap-4 p-4", "rounded-md border border-solid border-transparent", "items-start", "bolt-font-body-s-regular", colors.background[severity], addConsumerClasses(state, className)),
    id,
    role: "region",
    "aria-describedby": titleId ? descriptionId : undefined,
    "aria-labelledby": titleId || descriptionId || undefined
  };
  const element = useRender({
    defaultTagName: "div",
    ref,
    render,
    state,
    props: mergeProps(defaultProps, otherComponentProps)
  });
  const contextValue = React.useMemo(() => ({
    severity,
    visible,
    setVisible,
    setDescriptionId,
    setTitleId,
    descriptionId,
    titleId,
    onVisibleChange
  }), [descriptionId, setVisible, severity, titleId, visible, onVisibleChange]);
  return /*#__PURE__*/React.createElement(AlertContext.Provider, {
    value: contextValue
  }, visible ? element : null);
});