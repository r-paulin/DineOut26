import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useAlertContext } from "../context/useAlertContext";
import { addConsumerClasses } from "../../utils/combine-classes";
import { cx } from "../../../../helpers/utils/cx";
export const AlertContent = /*#__PURE__*/React.forwardRef(function AlertContentWrapper(props, ref) {
  const {
    className,
    render,
    ...otherComponentProps
  } = props;
  const {
    severity,
    visible
  } = useAlertContext();
  const state = {
    severity,
    visible
  };
  const defaultProps = {
    className: cx("flex w-full flex-col items-start gap-2", addConsumerClasses(state, className)),
    role: severity === "error" ? "alert" : "status",
    "aria-live": severity === "error" ? "assertive" : "polite"
  };
  return useRender({
    ref,
    defaultTagName: "div",
    render,
    state,
    props: mergeProps(defaultProps, otherComponentProps)
  });
});