function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { CheckboxGroup as BaseUICheckboxGroup } from "@base-ui/react";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * Checkbox Group is composed together with Checkbox. Import the components and place them together:
 *
 * ```tsx
 * <CheckboxGroup>
 *   <Checkbox
 *     name="field-name"
 *     value="option-1"
 *   />
 *   <Checkbox
 *     name="field-name"
 *     value="option-2"
 *   />
 *   <Checkbox
 *     name="field-name"
 *     value="option-3"
 *   />
 * </CheckboxGroup>
 * ```
 * A `render` prop is provided for the `CheckboxGroup` component to
 * allow custom rendering of the component.
 *
 * See `Field` and `Form` component documentation for better understanding of the field composition in a form.
 */
export const CheckboxGroup = /*#__PURE__*/React.forwardRef(function CheckboxGroup(props, ref) {
  const {
    children,
    className,
    ...restProps
  } = props;
  const checkboxGroupClassname = React.useCallback(state => {
    return cx("flex flex-col items-start gap-2", addConsumerClasses(state, className));
  }, [className]);
  return /*#__PURE__*/React.createElement(BaseUICheckboxGroup, _extends({}, restProps, {
    className: checkboxGroupClassname,
    ref: ref
  }), children);
});