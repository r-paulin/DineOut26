import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
/**
 * ### Autocomplete.Value
 * Renders the current string value of the autocomplete.
 * Doesn't render its own HTML element.
 *
 * ```tsx
 * <Autocomplete.Value>
 *   {(value) => <span>{value}</span>}
 * </Autocomplete.Value>
 * ```
 */
export declare function AutocompleteValue(props: BaseUIAutocomplete.Value.Props): React.ReactElement;
//# sourceMappingURL=AutocompleteValue.d.ts.map