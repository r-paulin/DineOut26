function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import Clear from "@bolteu/kalep-react-icons/dist/Clear";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useAutocompleteContextSelector, useAutocompleteInputContextSelector } from "./context";

/**
 * ### Autocomplete.Clear
 * Optional affordance for clearing the current input value.
 *
 * ```tsx
 * <Autocomplete.Controls>
 *   <Autocomplete.Clear aria-label="Clear input" />
 *   <Autocomplete.Trigger>…</Autocomplete.Trigger>
 * </Autocomplete.Controls>
 * ```
 */
export const AutocompleteClear = /*#__PURE__*/React.forwardRef(function AutocompleteClear(props, forwardedRef) {
  const {
    "aria-label": ariaLabel,
    className,
    render,
    ...rest
  } = props;
  const size = useAutocompleteContextSelector(context => context.size);
  const readonly = useAutocompleteInputContextSelector(context => context.autocompleteInputProps?.readOnly ?? false);
  const getAutocompleteClearClassNames = React.useCallback(state => cx("block text-tertiary", state.disabled ? "cursor-not-allowed" : `
                      cursor-pointer
                      hover:text-primary
                    `, addConsumerClasses(state, className)), [className]);
  const renderDefault = React.useCallback((renderProps, state) => {
    const {
      onMouseDown,
      onClick,
      className: clearClassName,
      ...remainingRenderProps
    } = renderProps;

    // Prevent the default onMouseDown behavior to avoid focus jumps
    const handleMouseDown = event => {
      event.preventDefault();
      event.stopPropagation();
      onMouseDown?.(event);
    };

    // Stop click from bubbling to the Input wrapper's onClick handler
    const handleClick = event => {
      event.stopPropagation();
      onClick?.(event);
    };
    if (state.disabled || readonly) {
      return /*#__PURE__*/React.createElement(React.Fragment, null);
    }
    return /*#__PURE__*/React.createElement("button", _extends({}, remainingRenderProps, {
      "aria-hidden": "true",
      type: "button",
      className: cx("border-0 bg-transparent p-0"),
      onMouseDown: handleMouseDown,
      onClick: handleClick
    }), /*#__PURE__*/React.createElement(Clear, {
      "aria-hidden": "true",
      className: clearClassName,
      size: size === "sm" ? "md" : "lg"
    }));
  }, [size, readonly]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.Clear, _extends({}, rest, {
    "aria-label": ariaLabel ?? "Clear input",
    ref: forwardedRef,
    className: getAutocompleteClearClassNames,
    render: render ?? renderDefault
  }));
});