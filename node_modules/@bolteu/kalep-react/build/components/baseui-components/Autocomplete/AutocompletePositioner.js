function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import { useAutocompleteContextSelector } from "./context";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Autocomplete.Positioner
 * Anchors `Autocomplete.Popup` relative to the input wrapper or a custom anchor.
 *
 * ```tsx
 * <Autocomplete.Portal>
 *   <Autocomplete.Positioner sideOffset={4}>
 *     <Autocomplete.Popup>…</Autocomplete.Popup>
 *   </Autocomplete.Positioner>
 * </Autocomplete.Portal>
 * ```
 *
 * Override `anchor` to customize placement; otherwise it falls back to the input wrapper.
 */
export const AutocompletePositioner = /*#__PURE__*/React.forwardRef(function AutocompletePositioner(props, forwardedRef) {
  const {
    className,
    anchor,
    ...rest
  } = props;
  const controlRef = useAutocompleteContextSelector(context => context.inputWrapperRef);
  const getAutocompletePositionerClassNames = React.useCallback(state => cx("max-h-80 w-max rounded-md shadow-md", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.Positioner, _extends({
    sideOffset: 5
  }, rest, {
    ref: forwardedRef,
    anchor: anchor ?? controlRef,
    className: getAutocompletePositionerClassNames
  }));
});