import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useAutocompleteInputContextSelector } from "./context";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
/**
 * ### Autocomplete.Controls
 * Flex container for clear buttons, triggers, or custom actions.
 * Place it directly inside `Autocomplete.Input`.
 *
 * ```tsx
 * <Autocomplete.Input>
 *   <Autocomplete.Control />
 *   <Autocomplete.Controls>
 *     <Autocomplete.Clear />
 *     <Autocomplete.Trigger>
 *       <Autocomplete.Icon />
 *     </Autocomplete.Trigger>
 *   </Autocomplete.Controls>
 * </Autocomplete.Input>
 * ```
 */
export const AutocompleteControls = /*#__PURE__*/React.forwardRef(function AutocompleteControls(props, forwardRef) {
  const {
    render,
    className,
    ...otherComponentProps
  } = props;
  const autocompleteInputState = useAutocompleteInputContextSelector(context => context.autocompleteInputState);
  const defaultProps = {
    className: cx("flex items-center gap-2", addConsumerClasses(autocompleteInputState, className))
  };
  return useRender({
    defaultTagName: "div",
    render,
    ref: forwardRef,
    state: {
      ...autocompleteInputState
    },
    props: mergeProps(defaultProps, otherComponentProps)
  });
});