function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Autocomplete.List
 * Scrollable container for option rows inside `Autocomplete.Popup`. Provide a function child to render
 * each item from the underlying collection.
 *
 * ```tsx
 * <Autocomplete.Portal>
 *   <Autocomplete.Positioner>
 *     <Autocomplete.Popup>
 *       <Autocomplete.List>
 *         {(item) => (
 *           <Autocomplete.Item key={item.value} value={item}>
 *             <span>{item.label}</span>
 *           </Autocomplete.Item>
 *         )}
 *       </Autocomplete.List>
 *     </Autocomplete.Popup>
 *   </Autocomplete.Positioner>
 * </Autocomplete.Portal>
 * ```
 */
export const AutocompleteList = /*#__PURE__*/React.forwardRef(function AutocompleteList(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getAutocompleteListClassNames = React.useCallback(state => cx(`m-0 max-h-80 cursor-pointer list-none overflow-y-auto overscroll-none px-0 py-1`, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.List, _extends({}, rest, {
    ref: forwardedRef,
    className: getAutocompleteListClassNames
  }));
});