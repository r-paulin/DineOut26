import * as React from "react";
const AutocompleteRootContext = /*#__PURE__*/React.createContext(null);
export function AutocompleteRootContextProvider({
  children,
  size
}) {
  const inputWrapperRef = React.useRef(null);
  const contextValue = React.useMemo(() => ({
    size,
    inputWrapperRef
  }), [size]);
  return /*#__PURE__*/React.createElement(AutocompleteRootContext.Provider, {
    value: contextValue
  }, children);
}
export function useAutocompleteRootContext() {
  const context = React.useContext(AutocompleteRootContext);
  if (!context) {
    throw new Error("Autocomplete parts must be rendered within Autocomplete.Root");
  }
  return context;
}
export function useAutocompleteContextSelector(selector) {
  const context = useAutocompleteRootContext();
  return selector(context);
}