function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { useAutocompleteInputContextSelector } from "./context";

/**
 * ### Autocomplete.Trigger
 * Button used to toggle the popup, typically wrapping `Autocomplete.Icon`.
 *
 * ```tsx
 * <Autocomplete.Controls>
 *   <Autocomplete.Trigger>
 *     <Autocomplete.Icon />
 *   </Autocomplete.Trigger>
 * </Autocomplete.Controls>
 * ```
 */
export const AutocompleteTrigger = /*#__PURE__*/React.forwardRef(function AutocompleteTrigger(props, forwardedRef) {
  const {
    className,
    children,
    render,
    ...rest
  } = props;
  const readOnly = useAutocompleteInputContextSelector(context => context.autocompleteInputProps?.readOnly ?? false);
  const defaultRender = React.useCallback((triggerProps, state) => {
    const autocompleteTriggerClassNames = cx("flex items-center border-0 bg-transparent p-0", state.disabled ? "pointer-events-none cursor-not-allowed" : readOnly ? "cursor-default" : "cursor-pointer", addConsumerClasses(state, className));
    return /*#__PURE__*/React.createElement("button", _extends({}, triggerProps, {
      "aria-hidden": readOnly,
      className: autocompleteTriggerClassNames,
      ref: triggerProps.ref,
      type: "button"
    }), children);
  }, [children, className, readOnly]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.Trigger, _extends({}, rest, {
    render: render ?? defaultRender,
    ref: forwardedRef
  }), children);
});