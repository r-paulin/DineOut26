function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Autocomplete.Popup
 * Surface that houses `Autocomplete.List`, `Autocomplete.Empty`, or custom footers.
 *
 * ```tsx
 * <Autocomplete.Positioner>
 *   <Autocomplete.Popup>
 *     <Autocomplete.Empty>No options found</Autocomplete.Empty>
 *     <Autocomplete.List>…</Autocomplete.List>
 *   </Autocomplete.Popup>
 * </Autocomplete.Positioner>
 * ```
 */
export const AutocompletePopup = /*#__PURE__*/React.forwardRef(function AutocompletePopup(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getAutocompletePopupClassNames = React.useCallback(state => cx(`
                  max-h-80 min-w-[var(--anchor-width)] max-w-[var(--anchor-width)] overflow-hidden rounded-md
                  bg-layer-floor-3 shadow-md
                `, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.Popup, _extends({}, rest, {
    ref: forwardedRef,
    className: getAutocompletePopupClassNames
  }));
});