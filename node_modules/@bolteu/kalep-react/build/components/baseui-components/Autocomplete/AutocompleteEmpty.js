function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Autocomplete.Empty
 * Placeholder content shown when the list has zero items.
 *
 * ```tsx
 * <Autocomplete.Popup>
 *   <Autocomplete.Empty>No results found</Autocomplete.Empty>
 *   <Autocomplete.List>…</Autocomplete.List>
 * </Autocomplete.Popup>
 * ```
 *
 * Combine with actions or hints to guide the user toward the next step.
 */
export const AutocompleteEmpty = /*#__PURE__*/React.forwardRef(function AutocompleteEmpty(props, forwardedRef) {
  const {
    className,
    children,
    ...rest
  } = props;
  const getAutocompleteEmptyClassNames = React.useCallback(state => cx(`
                  flex items-center px-6 text-primary
                  [&:not(:empty)]:pt-2
                `, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.Empty, _extends({}, rest, {
    ref: forwardedRef,
    className: getAutocompleteEmptyClassNames
  }), children);
});