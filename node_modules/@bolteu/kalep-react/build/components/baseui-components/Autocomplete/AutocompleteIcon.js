function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Autocomplete as BaseUIAutocomplete } from "@base-ui/react/autocomplete";
import ChevronDown from "@bolteu/kalep-react-icons/dist/ChevronDown";
import { useAutocompleteContextSelector, useAutocompleteInputContextSelector } from "./context";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Autocomplete.Icon
 * Chevron/indicator rendered inside `Autocomplete.Trigger`.
 *
 * ```tsx
 * <Autocomplete.Trigger>
 *   <Autocomplete.Icon />
 * </Autocomplete.Trigger>
 * ```
 */
export const AutocompleteIcon = /*#__PURE__*/React.forwardRef(function AutocompleteIcon(props, forwardedRef) {
  const size = useAutocompleteContextSelector(context => context.size);
  const disabled = useAutocompleteInputContextSelector(context => context.autocompleteInputState?.disabled);
  const open = useAutocompleteInputContextSelector(context => context.autocompleteInputState?.open);
  const {
    className,
    render,
    ...rest
  } = props;
  const getAutocompleteIconClassNames = React.useCallback(state => cx(`transition-transform duration-100 ease-linear`, open && "rotate-180", disabled ? "text-tertiary" : "text-primary", addConsumerClasses(state, className)), [className, disabled, open]);
  const renderDefault = React.useCallback(iconProps => {
    return /*#__PURE__*/React.createElement(ChevronDown, _extends({}, iconProps, {
      size: size === "sm" ? "md" : "lg",
      "aria-hidden": "true"
    }));
  }, [size]);
  return /*#__PURE__*/React.createElement(BaseUIAutocomplete.Icon, _extends({}, rest, {
    ref: forwardedRef,
    className: getAutocompleteIconClassNames,
    render: render ?? renderDefault
  }));
});