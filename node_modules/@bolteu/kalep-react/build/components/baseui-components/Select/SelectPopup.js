function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.Popup
 * Scrollable container rendered inside the positioner. Its width is anchored to
 * the trigger width via CSS custom properties, and its maximum height is capped
 * at `20rem` to keep long lists manageable.
 */
export const SelectPopup = /*#__PURE__*/React.forwardRef(function SelectPopup(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getSelectPopupClassNames = React.useCallback(state => cx(`
                  max-h-80 min-w-[var(--anchor-width)] max-w-[var(--anchor-width)] overflow-hidden rounded-md
                  bg-layer-floor-3 shadow-md
                `, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUISelect.Popup, _extends({}, rest, {
    className: getSelectPopupClassNames,
    ref: forwardedRef
  }));
});