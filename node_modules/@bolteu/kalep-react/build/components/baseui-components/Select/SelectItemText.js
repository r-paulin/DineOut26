function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.ItemText
 * Primary text label inside a `Select.Item`. Its content is also mirrored into
 * `Select.Value` when the item is selected, so it should contain the
 * human-readable display string for the option.
 */
export const SelectItemText = /*#__PURE__*/React.forwardRef(function SelectItemText(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getSelectItemTextClassNames = React.useCallback(state => cx("bolt-font-body-m-regular m-0 min-w-0 flex-1 text-primary", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUISelect.ItemText, _extends({}, rest, {
    ref: forwardedRef,
    className: getSelectItemTextClassNames
  }));
});