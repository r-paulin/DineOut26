import * as React from "react";
/**
 * ### Select.Positioner
 * Floating positioner that anchors the popup relative to the trigger. Defaults
 * to a 5 px side offset with item-alignment disabled so the popup aligns to the
 * trigger edge rather than the selected item.
 */
export declare const SelectPositioner: React.ForwardRefExoticComponent<Omit<import("@base-ui/react/select").SelectPositionerProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=SelectPositioner.d.ts.map