function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.Item
 * A single option row inside `Select.List`. Applies highlighted and disabled
 * visual states automatically. Compose with `Select.ItemText` and optionally
 * `Select.ItemIndicator` or `Select.CheckboxIndicator` for richer layouts.
 */
export const SelectItem = /*#__PURE__*/React.forwardRef(function SelectItem(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getSelectItemClassNames = React.useCallback(state => cx("flex min-h-12 w-full items-center gap-3 px-4 py-3 outline-none", {
    "hover:bg-neutral-secondary focus:hover:bg-neutral-secondary": !state.disabled,
    "bg-neutral-secondary": state.highlighted,
    "cursor-not-allowed": state.disabled
  }, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUISelect.Item, _extends({}, rest, {
    className: getSelectItemClassNames,
    ref: forwardedRef
  }));
});