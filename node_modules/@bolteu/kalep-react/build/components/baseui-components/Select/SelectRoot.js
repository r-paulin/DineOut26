function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { SelectContextProvider } from "./context/SelectContextProvider";
/**
 * ### Select.Root
 * Root provider for the Select compound component. Accepts all Base UI `Select.Root` props
 * plus two custom props: `size` (`"sm" | "md" | "lg"`, default `"md"`) controls trigger
 * height and typography, and `multiline` (default `false`) allows the selected value to wrap
 * across multiple lines.
 *
 * ```tsx
 * <Select.Root>
 *   <Select.Trigger>
 *     <Select.Value placeholder="Pick an option" />
 *     <Select.Icon />
 *   </Select.Trigger>
 *   <Select.Portal>
 *     <Select.Positioner>
 *       <Select.Popup>
 *         <Select.List>
 *           <Select.Item value="a">
 *             <Select.ItemText>Option A</Select.ItemText>
 *             <Select.ItemIndicator />
 *           </Select.Item>
 *         </Select.List>
 *       </Select.Popup>
 *     </Select.Positioner>
 *   </Select.Portal>
 * </Select.Root>
 * ```
 */
export function SelectRoot(props) {
  const {
    size = "md",
    multiline = false,
    disabled,
    readOnly = false,
    multiple,
    ...rest
  } = props;
  return /*#__PURE__*/React.createElement(BaseUISelect.Root, _extends({
    disabled: disabled,
    readOnly: readOnly,
    multiple: multiple
  }, rest), /*#__PURE__*/React.createElement(SelectContextProvider, {
    customSelectProps: {
      size,
      multiline,
      multiple: multiple === true
    },
    selectOwnProps: {
      disabled,
      readOnly
    }
  }, props.children));
}