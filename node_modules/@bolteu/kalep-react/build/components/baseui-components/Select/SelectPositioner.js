function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.Positioner
 * Floating positioner that anchors the popup relative to the trigger. Defaults
 * to a 5 px side offset with item-alignment disabled so the popup aligns to the
 * trigger edge rather than the selected item.
 */
export const SelectPositioner = /*#__PURE__*/React.forwardRef(function SelectPositioner(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getSelectPositionerClassNames = React.useCallback(state => cx("max-h-80 w-max rounded-md shadow-md", addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUISelect.Positioner, _extends({
    alignItemWithTrigger: false,
    sideOffset: 5
  }, rest, {
    ref: forwardedRef,
    className: getSelectPositionerClassNames
  }));
});