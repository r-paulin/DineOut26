import * as React from "react";
/**
 * ### Select.ItemText
 * Primary text label inside a `Select.Item`. Its content is also mirrored into
 * `Select.Value` when the item is selected, so it should contain the
 * human-readable display string for the option.
 */
export declare const SelectItemText: React.ForwardRefExoticComponent<Omit<import("@base-ui/react/select").SelectItemTextProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=SelectItemText.d.ts.map