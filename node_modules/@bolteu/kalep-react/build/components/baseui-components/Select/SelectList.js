function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.List
 * Scrollable list that wraps all `Select.Item` elements inside the popup.
 * Handles overflow scrolling and collapses its vertical padding when empty.
 */
export const SelectList = /*#__PURE__*/React.forwardRef(function SelectList(props, forwardedRef) {
  const {
    className,
    ...rest
  } = props;
  const getSelectListClassNames = React.useCallback(state => cx(`
                  m-0 max-h-80 cursor-pointer list-none overflow-y-auto overscroll-none px-0 py-1
                  empty:py-0
                `, addConsumerClasses(state, className)), [className]);
  return /*#__PURE__*/React.createElement(BaseUISelect.List, _extends({}, rest, {
    className: getSelectListClassNames,
    ref: forwardedRef
  }));
});