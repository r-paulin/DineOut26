function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import Check from "@bolteu/kalep-react-icons/dist/Check";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.ItemIndicator
 * Renders a `Check` icon inside a `Select.Item` when that item is selected,
 * styled in the positive-primary color.
 */
export const SelectItemIndicator = /*#__PURE__*/React.forwardRef(function SelectItemIndicator(props, forwardedRef) {
  const {
    render,
    className,
    ...rest
  } = props;
  const getSelectItemIndicatorClassNames = React.useCallback(state => cx("flex-shrink-0 text-action-secondary", addConsumerClasses(state, className)), [className]);
  const renderDefault = React.useCallback(props => {
    // ref is not forwarded to the Kalep icons
    const {
      ref: _ref,
      ...rest
    } = props;
    return /*#__PURE__*/React.createElement(Check, _extends({
      "aria-hidden": "true",
      size: "md"
    }, rest));
  }, []);
  return /*#__PURE__*/React.createElement(BaseUISelect.ItemIndicator, _extends({}, rest, {
    ref: forwardedRef,
    render: render ?? renderDefault,
    className: getSelectItemIndicatorClassNames
  }));
});