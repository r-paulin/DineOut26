function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Select as BaseUISelect } from "@base-ui/react/select";
import { CLASSNAME_TW_CHECKBOX_INDICATOR_BASE, getCheckboxIndicatorStateClass, renderCheckboxIndicatorDefault } from "../utils/checkbox-indicator";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";

/**
 * ### Select.CheckboxIndicator
 * Checkbox-style selection indicator for use inside `Select.Item` in
 * multi-select scenarios. Renders a filled square with a `Check` icon when
 * selected, and an outlined square when unselected. Stays mounted in the DOM
 * regardless of selection state to enable CSS transitions.
 */
export const SelectCheckboxIndicator = /*#__PURE__*/React.forwardRef(function SelectCheckboxIndicator(props, ref) {
  const {
    render,
    className,
    ...rest
  } = props;
  const getCheckboxIndicatorClassNames = React.useCallback(state => cx(CLASSNAME_TW_CHECKBOX_INDICATOR_BASE, getCheckboxIndicatorStateClass(state.selected), addConsumerClasses(state, className)), [className]);
  const defaultRender = React.useCallback((renderProps, state) => renderCheckboxIndicatorDefault(renderProps, state.selected), []);
  return /*#__PURE__*/React.createElement(BaseUISelect.ItemIndicator, _extends({
    keepMounted: true
  }, rest, {
    ref: ref,
    className: getCheckboxIndicatorClassNames,
    render: render ?? defaultRender
  }));
});