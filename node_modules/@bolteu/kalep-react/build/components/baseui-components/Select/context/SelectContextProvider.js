import * as React from "react";
export const SelectContext = /*#__PURE__*/React.createContext(null);
export function SelectContextProvider({
  children,
  customSelectProps,
  selectOwnProps
}) {
  const contextValue = React.useMemo(() => ({
    customSelectProps,
    selectOwnProps
  }), [customSelectProps, selectOwnProps]);
  return /*#__PURE__*/React.createElement(SelectContext.Provider, {
    value: contextValue
  }, children);
}