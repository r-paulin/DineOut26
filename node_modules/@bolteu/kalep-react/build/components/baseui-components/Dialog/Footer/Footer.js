import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useDialogPopup } from "../context/useDialogPopup";
import { addConsumerClasses } from "../../utils/combine-classes";
import { cx } from "../../../../helpers/utils/cx";
export const DialogFooterComponent = /*#__PURE__*/React.forwardRef(function DialogFooterComponent(props, ref) {
  const {
    render,
    className,
    ...otherComponentProps
  } = props;
  const {
    dialogPopupState
  } = useDialogPopup();
  const defaultProps = {
    className: cx("px-6 pb-6 pt-4", "flex justify-end gap-4", addConsumerClasses(dialogPopupState, className))
  };
  return useRender({
    defaultTagName: "footer",
    render,
    ref,
    state: {
      ...dialogPopupState
    },
    props: mergeProps(defaultProps, otherComponentProps)
  });
});