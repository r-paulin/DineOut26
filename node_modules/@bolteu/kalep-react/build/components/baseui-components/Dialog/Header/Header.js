import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { useDialogPopup } from "../context/useDialogPopup";
import { addConsumerClasses } from "../../utils/combine-classes";
import { cx } from "../../../../helpers/utils/cx";
export const DialogHeaderComponent = /*#__PURE__*/React.forwardRef(function DialogHeaderComponent(props, ref) {
  const {
    render,
    className,
    ...otherComponentProps
  } = props;
  const {
    dialogPopupState
  } = useDialogPopup();
  const defaultProps = {
    className: cx("flex items-baseline justify-between gap-6 px-6 pb-4 pt-5", addConsumerClasses(dialogPopupState, className))
  };
  return useRender({
    defaultTagName: "header",
    render,
    ref,
    state: {
      ...dialogPopupState
    },
    props: mergeProps(defaultProps, otherComponentProps)
  });
});