import * as React from "react";
export const DialogPopupContext = /*#__PURE__*/React.createContext(null);
export function DialogPopupProvider(props) {
  const {
    children,
    dialogPopupState
  } = props;
  const contextValue = React.useMemo(() => ({
    dialogPopupState
  }), [dialogPopupState]);
  return /*#__PURE__*/React.createElement(DialogPopupContext.Provider, {
    value: contextValue
  }, children);
}