import * as React from "react";
import { mergeProps, useRender } from "@base-ui/react";
import { addConsumerClasses } from "../utils/combine-classes";
import { cx } from "../../../helpers/utils/cx";
import { TypographyStackProvider } from "./context/TypographyStackContext";
export const TypographyStackRoot = /*#__PURE__*/React.forwardRef(function TypographyStackRoot(props, ref) {
  const {
    children,
    className,
    render,
    variant = "base",
    ...otherComponentProps
  } = props;
  const state = React.useMemo(() => ({
    variant
  }), [variant]);
  const defaultProps = {
    className: cx("flex flex-col", {
      "gap-1": variant === "sm-expressive"
    }, addConsumerClasses(state, className)),
    children: /*#__PURE__*/React.createElement(TypographyStackProvider, {
      value: {
        variant
      }
    }, children)
  };
  return useRender({
    ref,
    render,
    defaultTagName: "div",
    state,
    props: mergeProps(defaultProps, otherComponentProps)
  });
});
TypographyStackRoot.displayName = "TypographyStack.Root";