import * as React from "react";
export const TypographyStackContext = /*#__PURE__*/React.createContext(null);
export function TypographyStackProvider(props) {
  return /*#__PURE__*/React.createElement(TypographyStackContext.Provider, {
    value: props.value
  }, props.children);
}
export function useTypographyStackContext() {
  const context = React.useContext(TypographyStackContext);
  if (!context) {
    throw new Error("TypographyStack components must be used within TypographyStack");
  }
  return context;
}