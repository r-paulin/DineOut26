function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { Typography } from "./Typography";
import { useTypographyStackContext } from "./context/TypographyStackContext";
import { typographyStackPresets } from "./typographyStackPresets";
import { cx } from "../../../helpers/utils/cx";
import { addConsumerClasses } from "../utils/combine-classes";
export const TypographyStackPrimary = /*#__PURE__*/React.forwardRef(function TypographyStackPrimary(props, ref) {
  const {
    className,
    ...restProps
  } = props;
  const {
    variant
  } = useTypographyStackContext();
  const defaultClassName = typographyStackPresets[variant].primary.className;
  const combinedClassName = addConsumerClasses({
    variant
  }, cx(defaultClassName, className));
  return /*#__PURE__*/React.createElement(Typography, _extends({}, restProps, {
    className: combinedClassName,
    ref: ref
  }));
});