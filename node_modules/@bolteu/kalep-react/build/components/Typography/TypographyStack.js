function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
import * as React from "react";
import { cx } from "../../helpers/utils/cx";
import { Typography } from "./Typography";
export function TypographyStack(props) {
  const {
    children,
    gap,
    primary,
    secondary,
    primaryTypographyProps,
    secondaryTypographyProps,
    isReverse,
    variant = "base"
  } = props;
  let primaryElement = primary ?? children;
  let secondaryElement = secondary;
  let gapValue = gap;
  const variantPrimaryProps = {};
  const variantSecondaryProps = {};
  switch (variant) {
    case "sm":
      {
        variantPrimaryProps.variant = "body-s-regular";
        variantPrimaryProps.color = "primary";
        variantSecondaryProps.variant = "body-xs-regular";
        variantSecondaryProps.color = "secondary";
        break;
      }
    case "sm-expressive":
      {
        variantPrimaryProps.variant = "body-s-regular";
        variantPrimaryProps.color = "primary";
        variantSecondaryProps.variant = "body-s-regular";
        variantSecondaryProps.color = "primary";
        gapValue = gapValue ?? 1;
        break;
      }
    case "base":
    default:
      {
        variantPrimaryProps.variant = "body-m-regular";
        variantPrimaryProps.color = "primary";
        variantSecondaryProps.variant = "body-s-regular";
        variantSecondaryProps.color = "secondary";
        break;
      }
  }
  if (primaryElement != null && !isTypographyComponent(primaryElement)) {
    primaryElement = /*#__PURE__*/React.createElement(Typography, _extends({}, variantPrimaryProps, primaryTypographyProps), primaryElement);
  }
  if (secondaryElement != null && !isTypographyComponent(secondaryElement)) {
    secondaryElement = /*#__PURE__*/React.createElement(Typography, _extends({}, variantSecondaryProps, secondaryTypographyProps), secondaryElement);
  }
  const inlineStyles = {
    ...(gapValue && {
      gap: getRemFromSpacingSize(gapValue)
    })
  };
  return /*#__PURE__*/React.createElement("div", {
    className: cx("flex", isReverse ? "flex-col-reverse" : "flex-col"),
    style: inlineStyles
  }, primaryElement, secondaryElement);
}
function isTypographyComponent(element) {
  return element != null && element.type === Typography;
}
function getRemFromSpacingSize(spacingValue) {
  return `${spacingValue / 4}rem`;
}