import * as React from "react";
import { cx } from "../../helpers/utils/cx";
const DEFAULT_PADDING = 5;
const elevationStyles = {
  "floor-1": "bg-layer-floor-1 border-solid border border-neutral-secondary",
  "floor-2": "bg-layer-floor-2 shadow-md",
  "floor-3": "bg-layer-floor-3 shadow-lg",
  surface: "bg-layer-surface",
  outlined: "bg-special-nulled border-solid border border-separator"
};
const cornerStyles = {
  rounded: "rounded-lg",
  square: "rounded-none"
};
export function Island({
  elevation = "floor-1",
  corners = "rounded",
  children,
  padding = DEFAULT_PADDING,
  paddingTop,
  paddingLeft,
  paddingBottom,
  paddingRight,
  className
}) {
  const containerStyle = React.useMemo(() => {
    return {
      ...(padding !== undefined && {
        padding: getRemFromSpacingSize(padding)
      }),
      ...(paddingTop !== undefined && {
        paddingTop: getRemFromSpacingSize(paddingTop)
      }),
      ...(paddingLeft !== undefined && {
        paddingLeft: getRemFromSpacingSize(paddingLeft)
      }),
      ...(paddingRight !== undefined && {
        paddingRight: getRemFromSpacingSize(paddingRight)
      }),
      ...(paddingBottom !== undefined && {
        paddingBottom: getRemFromSpacingSize(paddingBottom)
      })
    };
  }, [padding, paddingLeft, paddingTop, paddingRight, paddingBottom]);
  return /*#__PURE__*/React.createElement("div", {
    style: containerStyle,
    className: cx(elevationStyles[elevation], cornerStyles[corners], className)
  }, children);
}
function getRemFromSpacingSize(spacingValue) {
  return `${spacingValue / 4}rem`;
}