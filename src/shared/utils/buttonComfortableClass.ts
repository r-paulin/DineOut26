/**
 * Comfortable CTA height — 56px (Figma). Use `size="lg"` on Kalep `<Button>`;
 * append {@link BUTTON_COMFORTABLE_HEIGHT_CLASS} when an override must win specificity.
 */
export const BUTTON_COMFORTABLE_HEIGHT_CLASS = "!h-14 !min-h-14"
