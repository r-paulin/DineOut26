import { modalImageUrl } from "@/shared/utils/publicImageUrls"

/** Figma `16942:16608` — cloche with X (active offer conflict). */
export const ACTIVE_OFFER_CONFLICT_HERO_SRC = modalImageUrl(
  "active-offer-conflict-hero.png",
)
