import { modalImageUrl } from "@/shared/utils/publicImageUrls"

/** Hero photo for claim / offer promo sheets (not post-claim success). */
export const CLAIM_PROMO_HERO_SRC = modalImageUrl("bg-modal.jpg")

/** Cloche + checkmark hero for legacy success / how-it-works sheets (Figma `16096:15217`). */
export const CLAIM_SUCCESS_HERO_SRC = modalImageUrl("photo.jpg")

/** Circular checkmark for fullscreen offer-claimed (Figma `17421:31561`). */
export const CLAIM_OFFER_CLAIMED_CHECKMARK_SRC = modalImageUrl(
  "offer-claimed-checkmark.png",
)
