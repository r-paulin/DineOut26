/**
 * OpenFreeMap **Liberty** — vector street map (roads, parks, water, POIs) in the
 * same spirit as Google Maps “Default” / roadmap. No API key; see
 * https://openfreemap.org and style attribution in the map.
 */
export const DISCOVER_STREET_MAP_STYLE_URL =
  "https://tiles.openfreemap.org/styles/liberty"
