/** Riga Old Town — near Doma laukums (Vecrīga; matches prototype restaurants). */
export const MAP_CENTER = { lat: 56.9493, lng: 24.1052 }

/** Mocked “you are here” — same as map center; restaurants ring around this point. */
export const MOCK_USER_LOCATION = MAP_CENTER

/** Default discovery map zoom (tight on user + nearby pins). */
export const MAP_DEFAULT_ZOOM = 16
