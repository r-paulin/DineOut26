/**
 * Full-bleed 8px section break between offer/promo blocks and venue (Consumer
 * Dine-out — Figma restaurant detail; green tint, not floor white).
 */
export function RestaurantDetailSectionDivider() {
  return (
    <div
      className="h-2 w-full shrink-0 bg-[rgba(0,45,30,0.07)]"
      aria-hidden
    />
  )
}
