/** Figma `[Dine-out] Mini-banner-Img` — claimed seal (`16084:49846`). */
export const OFFER_BANNER_MINI_CLAIMED_SRC =
  "/images/offer-banner/mini-banner-claimed.png"

/** Figma `[Dine-out] Mini-banner-Img` — unclaimed discount seal (`19056:141599`). */
export const OFFER_BANNER_MINI_UNCLAIMED_BASE_SRC =
  "/images/offer-banner/mini-banner-unclaimed-base.png"

/** Optional overlay on the unclaimed seal (same Figma asset stack). */
export const OFFER_BANNER_MINI_UNCLAIMED_OVERLAY_SRC =
  "/images/offer-banner/mini-banner-unclaimed-overlay.png"

/** Expired ALT uses the unclaimed stack with CSS greyscale (`19056:142290`). */
