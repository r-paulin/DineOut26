import { modalImageUrl } from "@/shared/utils/publicImageUrls"

/** Figma `16275:26856` — cloche + clock on mint background. */
export const VENUE_CLOSED_HERO_SRC = modalImageUrl("venue-closed-hero.png")
